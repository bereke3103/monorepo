import { Outlet } from 'react-router-dom';
// import SvgComponent from '@/assets/pic-svg.svg';

const App = () => {
  return (
    <div>
      SHOP MODULES PARENT
      <Outlet />
    </div>
  );
};

export default App;
